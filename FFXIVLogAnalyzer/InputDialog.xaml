<Window x:Class="FFXIVLogAnalyzer.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}" Height="160" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#1E1E2E" Foreground="#CDD6F4">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="PromptText" Text="请输入Boss技能ID或效果ID:" Foreground="#A6ADC8" Margin="0,0,0,8"/>
        <TextBox x:Name="InputBox" Grid.Row="1" Background="#313244" Foreground="#CDD6F4"
                 BorderBrush="#45475A" Padding="8,6" FontSize="14" Margin="0,0,0,12"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="查询" Click="Ok_Click" Background="#89B4FA" Foreground="#1E1E2E"
                    FontWeight="Bold" Padding="16,6" BorderThickness="0" Margin="0,0,8,0"/>
            <Button Content="取消" Click="Cancel_Click" Background="#45475A" Foreground="#CDD6F4"
                    Padding="16,6" BorderThickness="0"/>
        </StackPanel>
    </Grid>
</Window>
